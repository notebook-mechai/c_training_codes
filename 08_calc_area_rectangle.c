int main()
{
    float length = 4.6;
    float width = 5.1;
    float area;
    float perimeter;

    area = length * width;
    perimeter = ((length + width) * 2);

    printf("Area is:%f\n", area);
    printf("Perimeter is: %f\n", perimeter);

}
